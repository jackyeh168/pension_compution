# 退休金最小初始資金計算器 — 產品需求文件 (PRD)

## 1. 產品概述

一個網頁應用程式，協助使用者計算退休時所需的**最小初始股票資產**，使其在 45 年退休期間內不會耗盡。使用者輸入生活費、實質報酬率與政府退休金，系統即時算出結果並以表格與圖表呈現。

## 2. 核心假設

| 項目 | 說明 |
|------|------|
| 資產性質 | 初始資金即為股票投資組合，所有提領皆從此組合支出 |
| 報酬率 | 採用**實質報酬率**（名目報酬率 − 通膨率），已扣除通膨影響 |
| 生活費 | 以今日幣值計算，**每年固定不變**（因報酬率已反映通膨） |
| 政府退休金 | 每年固定金額，不隨通膨調整 |
| 退休期間 | 固定 45 年 |
| 提領時點 | 每年年初提領當年度生活費 |
| 投報時點 | 提領後剩餘資產於年末按實質報酬率成長 |

## 3. 輸入參數

| 參數 | 說明 | 預設值 | 單位 |
|------|------|--------|------|
| 每年生活費 | 退休後每年所需支出（今日幣值） | 600,000 | 元 |
| 實質報酬率 | 扣除通膨後的年化投資報酬率 | 5 | % |
| 每年政府退休金 | 政府每年發放的固定退休金 | 100,000 | 元 |

## 4. 計算方式

### 4.1 逐年模擬公式

給定初始資金 `C₀`，對第 `t` 年（t = 1, 2, ..., 45）：

```
每年從股票提領 W = max(0, 每年生活費 − 政府退休金)

年初資產   Aₜ = 上一年年末資產（A₀ = C₀）
提領後資產 Aₜ' = Aₜ − W
年末資產   Aₜ₊₁ = Aₜ' × (1 + r)
```

其中 `r` 為實質報酬率。

因為生活費與政府退休金皆為固定值，`W` 每年相同。

### 4.2 數值範例

以預設參數為例（生活費 60 萬、實質報酬率 5%、政府退休金 10 萬）：

```
W = max(0, 600,000 − 100,000) = 500,000 元/年

第 1 年：
  年初資產    = 9,331,387（最小初始資金）
  提領後資產  = 9,331,387 − 500,000 = 8,831,387
  年末資產    = 8,831,387 × 1.05 = 9,272,956

第 2 年：
  年初資產    = 9,272,956
  提領後資產  = 9,272,956 − 500,000 = 8,772,956
  年末資產    = 8,772,956 × 1.05 = 9,211,604

...（持續 45 年）

第 45 年：
  年末資產    ≈ 0 元（剛好耗盡）
```

### 4.3 最小初始資金求解 — 二分法

目標：找出最小的 `C₀`，使得 45 年模擬中**每年年末資產皆 ≥ 0**。

```
下界 lower = 0
上界 upper = 每年生活費 × 45（無投報的最差情境）

重複：
  mid = (lower + upper) / 2
  若 simulate(mid) 每年皆存活 → upper = mid
  否則                        → lower = mid
  直到 upper − lower < 0.5

最小初始資金 = round(upper)
```

精度：收斂至 0.5 元內，最終四捨五入至整數。

### 4.4 敏感度分析

固定生活費與政府退休金，分別以 **3%、使用者設定值、7%** 三種實質報酬率執行二分法，比較最小初始資金的變化。

若使用者設定值恰好為 3% 或 7%，則該情境不重複顯示。

## 5. 輸出內容

| 區域 | 內容 |
|------|------|
| **主要指標** | 最小初始資金，大字醒目顯示（格式：X,XXX,XXX 元） |
| **前 5 年表格** | 每列：年度、年初資產、當年生活費、政府退休金、從股票提領、報酬後資產 |
| **第 45 年結果** | 結束時剩餘資金（應趨近 0） |
| **走勢圖** | 45 年資產變化折線圖 |
| **敏感度分析** | 不同實質報酬率下的最小初始資金對照表 |

## 6. 技術架構

| 項目 | 選擇 |
|------|------|
| 語言 | Go |
| 網頁框架 | 標準函式庫 `net/http` |
| 前端 | 嵌入式 HTML + vanilla JavaScript |
| 圖表 | Chart.js (CDN) |
| 部署 | 單一二進位檔，預設 port 8080（可透過 `PORT` 環境變數覆寫） |

### 檔案結構

```
pension_compution/
├── PRD.md               # 本文件
├── go.mod               # Go 模組定義
├── pension.go           # 核心計算引擎（Simulate、FindMinimumCapital）
├── main.go              # HTTP 伺服器、API handler
└── templates/
    └── index.html       # 前端 UI
```

### API 規格

**POST `/api/calc`**

Request body：
```json
{
  "annualExpense": 600000,
  "realReturnRate": 0.05,
  "govtPension": 100000
}
```

Response body：
```json
{
  "minCapital": 9331387,
  "records": [
    {
      "year": 1,
      "beginAsset": 9331387,
      "livingCost": 600000,
      "govtPension": 100000,
      "stockWithdrawn": 500000,
      "endAsset": 9272956.35
    }
  ],
  "sensitivity": [
    { "returnRate": 0.03, "minCapital": 12627137 },
    { "returnRate": 0.05, "minCapital": 9331387 },
    { "returnRate": 0.07, "minCapital": 7278954 }
  ]
}
```

## 7. UI 版面

```
┌─────────────┬──────────────────────────────────────┐
│  側邊欄      │  主內容區                              │
│             │                                      │
│ 每年生活費   │  ┌──────────────────────────────────┐ │
│ [600,000]   │  │ 最小初始資金 = 9,331,387 元       │ │
│             │  └──────────────────────────────────┘ │
│ 實質報酬率   │                                      │
│ [5] %       │  前 5 年模擬明細                      │
│             │  ┌──┬────────┬──────┬──────┬────────┐ │
│ 政府退休金   │  │年│年初資產 │生活費│退休金│報酬後  │ │
│ [100,000]   │  ├──┼────────┼──────┼──────┼────────┤ │
│             │  │..│  ...   │ ...  │ ...  │  ...   │ │
│ [計算]      │  └──┴────────┴──────┴──────┴────────┘ │
│             │                                      │
│             │  第 45 年結果                          │
│             │  45 年資產走勢圖 📈                    │
│             │  敏感度分析表                          │
└─────────────┴──────────────────────────────────────┘
```

所有文字皆使用繁體中文。RWD 支援行動裝置（側邊欄自動切為上方）。
